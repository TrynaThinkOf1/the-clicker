# The-Clicker

## TODO: Fix the file handling in macro_storage, its really super messy and inefficient
### Doing this right now

## This is theoretically installable, and this installation guide is off the top of my head
### The only thing it does right now is very basic left/right/double-left/double-right click every min/sec/ms at a certain (x,y)
```bash
# MacOS
brew install gtk4

# Ubuntu/Debian
sudo apt install libgtk-4-dev

# Windows
fuck you

git clone https://www.github.com/trynathinkof1/the-clicker
cd the-clicker

make # hope for the best

./clicker
```